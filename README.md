# Bluex Test E2E Space

This repository is used by Bluex end-to-end tests.

Multi-issue flow tests use independent child issues with explicit dependencies so each child can land through its own PR while the parent waits for all required children.
